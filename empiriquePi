import math
import random

a=100000
incircle = 0
for i in range (a):    
    x=random.random()
    x=2*x-1    
    y=random.random()
    y=2*y-1    
    if ((x**2)+(y**2)<=1):
        incircle+=1
    i+=1    
pi = 4*(incircle/a)
print(pi)
